# Mac AI 会议纪要应用 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-17 |
| 产品名称 | Mac AI 会议纪要（暂定名） |
| 文档状态 | 根据设计讨论整理，包含已确认需求、设计建议和待验证事项 |
| 产品形态 | 原生 macOS 桌面应用，常驻菜单栏 |
| 目标环境 | 用户当前使用 macOS Tahoe 26.2.2；第一版以 macOS 26 为支持基线 |
| 主要服务 | AWS Transcribe、Amazon Bedrock |
| 当前阶段 | 需求与设计；本文件不代表已完成开发或服务验证 |

## 1. 产品背景与目标

用户在 Mac 上通过 Teams、Zoom 参加会议，希望应用在本人确认后记录会议声音和自己的发言，实时转录为文本，区分参会者，并在会后自动校对转录、生成会议纪要。

产品应减少用户在会议中手动记录和会后整理的工作，同时保留原始转录、人工修改以及结论对应的原话，方便核对。

### 1.1 目标用户

第一版面向个人使用者，使用自己的 Mac 和已经配置的 AWS 凭证。优先验证 Teams、Zoom 的 macOS 桌面客户端。

浏览器会议、其他会议软件的支持范围，需要通过后续兼容性验证确定，不作为首版已验证能力。

### 1.2 产品目标

1. 完整采集会议应用的远端声音与用户麦克风，清晰显示两路采集状态。
2. 默认处理中英混合会议，也允许用户切换为中文或英文。
3. 自动区分远端发言人，支持人工命名、备注和纠正归属。
4. 对原始转录进行可追溯的 LLM 校对，保留原意与不确定信息。
5. 生成包含议题、决策、行动项和待确认问题的纪要，并关联原文。
6. 在本地管理会议资料，支持编辑、搜索和导出。

### 1.3 产品边界

- 原生 macOS 应用是已确认的技术方向；第一版不交付 Windows、Linux 或移动端。
- 应用在本地运行和保存会议资料，但音频识别、文本校对和总结依赖 AWS 云端服务。
- 说话人分离只区分声音来源，不承诺自动获得 Teams、Zoom 中的真实姓名。
- 不承诺全离线识别、完全准确的转录或完全准确的说话人分离。
- 第一版不包含跨会议声纹身份识别、会议机器人自动入会、团队协作、自动发送纪要、日历同步或跨设备同步。

## 2. 已确认的产品决策

| 项目 | 已确认内容 |
|---|---|
| 平台 | 原生 macOS；SwiftUI 为主，必要时使用 AppKit |
| 启动方式 | 检测疑似会议后提醒，由用户确认开始；同时保留手动开始入口 |
| 默认识别语言 | 中英混合，可切换中文、英文 |
| 音频来源 | 会议软件输出声音和用户麦克风，分别采集 |
| 转录服务 | AWS Transcribe |
| 说话人 | 本人显示为“我”；远端显示为“发言人 A、B、C……” |
| 人工编辑 | 发言人命名、人物备注、发言归属纠正、转录编辑和内容备注 |
| LLM 处理 | 先校对转录，再生成会议纪要 |
| 默认模型 | 校对、总结均为 Bedrock 上的 GPT-6 Astra |
| 默认推理强度 | 两个阶段均明确设置为 medium |
| 可选模型 | GPT-5.6 Sol、GPT-5.6 Terra、GPT-5.6 Luna |
| 模型配置 | 校对和总结可分别配置模型及推理强度 |
| 数据保存 | 原始转录保留；人工修改优先；校对及纪要保留版本 |
| 纪要语言 | 默认中文，可切换英文；转录保留发言原语言 |
| 导出 | 第一版支持 Markdown、TXT |

本 PRD 中的优先级、状态细节和验收方法是对已确认需求的实施建议。尚未确认的默认值集中列在第 12 节。

## 3. 典型场景

### 3.1 中文或中英混合会议

用户加入 Teams 会议，收到记录提醒。确认后，应用采集远端声音和麦克风，显示实时转录。用户将“发言人 A”命名为“张经理”，并备注“客户方，负责采购”。会议结束后，应用先校对专有名词和明显错字，再生成中文纪要。

### 3.2 全英文会议

用户在开始前将识别语言切换为英文。转录保留英文原话，纪要可以选择中文或英文，不因纪要语言变化而覆盖原文。

### 3.3 人工纠正与重新生成

应用将同一位参会者识别为两个发言人。用户合并标签，修改一处转录错误，再重新生成纪要。人工修改必须保留，旧纪要仍可查看，新纪要使用最新的转录与发言人信息。

### 3.4 网络中断

会议期间网络中断。如果已开启本地音频缓存，应用继续保存授权范围内的音频，并标记待转录区间。恢复后继续实时转录，再处理积压内容。没有可用缓存的区间明确标记为缺失。

## 4. 用户流程与会议状态

主流程：

**待机 → 疑似会议提醒 → 用户确认 → 采集与实时转录 → 结束记录 → 转录收尾 → LLM 校对 → 生成纪要 → 查看、编辑与导出**

| 状态 | 用户可见行为 | 关键规则 |
|---|---|---|
| 待机 | 菜单栏显示未记录 | 不采集、保存或上传会议音频 |
| 疑似会议 | 提醒是否开始记录 | 检测只使用必要的运行状态、音频活动状态等信号；不以音频内容分析作为确认前的检测方式 |
| 启动准备 | 展示采集来源、语言和权限状态 | 完成用户确认与必要权限授权后才开始采集 |
| 记录中 | 显示时长、两路音量、实时转录 | 持续保存已确定的转录，显示云端连接状态 |
| 已暂停 | 显示暂停状态和时间区间 | 暂停期间不继续采集、缓存或上传新音频；已发送的结果可完成接收 |
| 转录待恢复 | 显示断网、凭证失效或其他原因 | 采集与转录状态分别展示；是否保留音频取决于缓存设置 |
| 转录收尾 | 停止采集，接收最后结果 | 尚未完成的区间必须有明确状态，不能直接显示“完整转录” |
| 校对中 | 显示校对进度 | 基于确定的转录版本工作，不覆盖人工编辑 |
| 纪要生成中 | 显示生成进度 | 使用校对稿及人工确认信息，保留原文引用 |
| 已完成 | 展示纪要、转录、人物与备注 | 允许编辑及重新校对、重新生成 |
| 部分完成／失败 | 展示已保存内容、失败阶段和重试入口 | 单个阶段失败不删除会议，也不使之前结果失效 |

### 4.1 开始与结束规则

- 检测到 Teams、Zoom 运行或有音频活动，不等于确认正在开会。
- 用户可以忽略提醒，也可以从菜单栏手动开始。
- 误报不能触发实际录音；漏报时必须能手动记录。
- 短暂安静、会议应用切到后台或窗口最小化，不自动结束记录。
- 会议应用退出或采集源消失时，提示用户处理，并分别显示受影响音轨的状态。
- 用户点击“结束并生成纪要”后，立即停止新音频采集，再处理已采集内容。
- 关闭会议主窗口不等于结束记录；菜单栏仍需清楚显示记录状态。
- 退出整个应用时，如仍在记录，必须让用户明确处理当前记录，避免误以为后台仍在录音。

## 5. 功能需求

优先级定义：

- **P0：** 第一版必须交付，或必须完成对应技术可行性验证。
- **P1：** 首版核心流程稳定后增强。

### 5.1 会议检测与手动记录

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| MEET-01 | P0 | 菜单栏提供手动开始、暂停、恢复和结束入口。 |
| MEET-02 | P0 | 检测疑似会议后发出提醒；用户确认前没有会议音频被采集、写入缓存或上传。 |
| MEET-03 | P0 | 相同疑似会话的提醒需去重；用户忽略后不连续打扰。具体抑制时长可配置或后续确定。 |
| MEET-04 | P0 | 开始前可查看会议应用、麦克风、识别语言，并编辑会议标题。 |
| MEET-05 | P0 | 会议结束后自动进入转录收尾、校对和总结流程；异常情况下保留手动重试。 |
| MEET-06 | P1 | 优化不同会议应用的检测准确率，扩展更多应用或浏览器会议。 |

### 5.2 音频采集与本地缓存

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| AUDIO-01 | P0 | 分别采集所选会议应用的输出与所选麦克风，避免先混成无法区分来源的单路音频。 |
| AUDIO-02 | P0 | 显示两路音量和采集状态，能够区分未授权、无声音、已暂停、来源不可用和采集异常。 |
| AUDIO-03 | P0 | 用户可以单独开关本应用的麦克风采集；界面明确说明其与 Teams、Zoom 的静音状态独立。 |
| AUDIO-04 | P0 | 支持暂停与恢复。时间戳保留暂停区间，不能把暂停前后错误拼接成连续发言。 |
| AUDIO-05 | P0 | 处理耳机插拔、蓝牙切换、默认设备变化和会议应用进程变化；不能静默漏录。 |
| AUDIO-06 | P0 | 提供本地音频缓存及保留期限设置；默认开启状态、保留期限见待确认项。 |
| AUDIO-07 | P0 | 断网时，缓存开启且可写入则继续保留音频；缓存关闭或不可用则明确显示缺失区间。 |
| AUDIO-08 | P0 | 缓存写入失败、磁盘空间不足或权限变化必须显示错误，不能继续显示“已完整保存”。 |
| AUDIO-09 | P0 | 验证耳机和外放场景。外放可能产生回声和重复转录，需要验证抑制方案及支持边界。 |
| AUDIO-10 | P1 | 增强外放回声抑制、复杂设备组合兼容性和录音诊断。 |

采集范围默认限制在用户选择的会议应用及麦克风。无法按预期采集指定应用时，不应静默扩大到整个系统音频。所选应用自身播放的通知等声音可能被一起采集，这一点需要在兼容性验证中记录。

回听功能仅在对应音频仍被保留时可用；音频到期删除后，原始转录、校对稿及纪要的文字引用仍应保留。

### 5.3 实时转录

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| ASR-01 | P0 | 使用 AWS Transcribe 流式识别。默认中英混合，支持手动选择中文或英文。 |
| ASR-02 | P0 | 区分服务返回的临时结果与确定结果，临时结果更新不能显示成多段重复发言。 |
| ASR-03 | P0 | 每段转录关联会议内时间、音频来源、服务会话和稳定的本地段落标识。 |
| ASR-04 | P0 | 已确定的原始转录增量保存，不能等到会议结束才保存。 |
| ASR-05 | P0 | 支持人工修改确定后的文本；修改作为独立记录保存，不覆盖原始服务结果。 |
| ASR-06 | P0 | 记录中切换识别语言，从切换点建立适当的新转录会话，会议内保留此前内容与时间关系。 |
| ASR-07 | P0 | 断线、重连和结果重发不能产生重复正文；缺失区间与待补转区间可区分。 |
| ASR-08 | P0 | 结束记录后接收最后的确定结果；超时或仍有缺口时允许重试，也允许明确以现有内容继续处理。 |

中英混合识别、远端说话人分离、双路并发转录的组合，必须在目标 AWS 区域和实际账户中验证。支持单项能力不等于已经验证该组合的实际效果。

### 5.4 说话人管理与人工备注

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| SPK-01 | P0 | 麦克风轨默认标记为“我”；会议软件轨开启说话人分离，显示“发言人 A、B、C……”。 |
| SPK-02 | P0 | 点击标签可设置姓名、角色和人物备注，整场会议中的人物显示同步更新。 |
| SPK-03 | P0 | 支持合并同一人的多个标签，保留原始映射与修改记录，并可撤销。 |
| SPK-04 | P0 | 支持将单段发言改归其他发言人；只修改所选范围。 |
| SPK-05 | P0 | 支持对发言添加文字备注、标记重点。备注与转录正文分别保存。 |
| SPK-06 | P0 | 人工指定的姓名和归属优先，不被后续自动处理直接覆盖。 |
| SPK-07 | P0 | 服务重连后返回的相同编号不能直接视为同一个人；不确定时建立新的临时标签，允许人工合并。 |
| SPK-08 | P1 | 提供批量调整归属、拆分发言片段和更方便的多人纠错操作。 |

人物身份只在当前会议内管理。用户麦克风采集到身边其他人的声音时，不能保证“我”这一标签仍准确，允许人工纠正。

### 5.5 会议术语表

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| TERM-01 | P0 | 支持录入当前会议的项目名、产品名、人员姓名、缩写及解释。 |
| TERM-02 | P0 | LLM 校对可参考术语表；术语表不能被当作会上已经说出的事实补入转录。 |
| TERM-03 | P1 | 支持常用术语模板，便于在多场会议中复用。 |

第一版术语表用于 LLM 校对，不预设所有语言都能使用相同的 Transcribe 自定义词汇功能。

### 5.6 LLM 转录校对

#### 处理时机与输入

第一版在会议结束、转录收尾后执行校对。输入包括确定的原始转录、已生效的人工编辑、发言人映射、术语表和必要的相邻上下文。会议中的临时字幕不逐条触发 LLM 校对。

长会议可以分段校对，每段保留相邻上下文、统一术语和原始段落标识。跨段结果必须去重，不能因分段漏掉发言。

#### 允许修改的范围

| 类别 | 处理规则 |
|---|---|
| 标点与断句 | 可自动形成校对稿，但不改变原意与时间归属 |
| 明显识别错字 | 有足够上下文依据时可修正，并记录修改 |
| 专有名词与中英文混合词 | 结合术语表与上下文修正；依据不足时提出建议 |
| 重复内容 | 仅处理可明确判断为识别或传输造成的重复，保留发言本身的重复强调 |
| 人名、金额、日期、数字、单位、否定词 | 无可靠依据的修改标记为待确认，不自动把猜测写成事实 |
| 含糊或缺失内容 | 保留原文及不确定性，不补写未识别出的事实 |
| 发言风格与承诺程度 | 不把犹豫、条件、建议或可能性改为确定承诺 |

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| COR-01 | P0 | 保存原始转录和校对稿，不以校对稿覆盖原始数据。 |
| COR-02 | P0 | 每项修改可以追溯到原文片段，包含修改前后内容、理由及处理状态。 |
| COR-03 | P0 | 提供修改对比，支持接受、撤销和人工编辑；低风险自动修改也可撤销。 |
| COR-04 | P0 | 人工编辑区域默认受保护，重新校对不能直接覆盖。 |
| COR-05 | P0 | 输出在应用侧验证段落标识、修改范围与原文匹配，拒绝无对应来源的修改。 |
| COR-06 | P0 | 校对不能擅自重排发言时间、修改说话人归属或插入人物备注作为原话。 |
| COR-07 | P0 | 校对失败时保留输入版本和已完成结果，支持重试；跳过校对继续总结必须明确标记。 |
| COR-08 | P1 | 增加会议中的阶段性校对，减少会后等待；不影响实时转录主流程。 |

校对不要求用户逐条确认所有修改后才能生成纪要。未确认且可能影响结论的内容，应在纪要中保留对应的不确定性。

LLM 校对读取的是文本，无法仅凭文本确定原始音频到底说了什么。需要回听的内容应保留时间定位，而不是通过提高推理强度假装获得确定答案。

### 5.7 会议纪要

默认输出结构：

| 部分 | 内容 |
|---|---|
| 会议信息 | 标题、时间、参会者显示名等已知信息 |
| 会议概览 | 简述会议目的与主要讨论内容 |
| 主要议题 | 按主题归纳关键讨论 |
| 已确认决策 | 区分最终决定、建议与尚未达成一致的事项 |
| 行动项 | 事项、负责人、截止日期及相关原文 |
| 待确认问题 | 分歧、缺失信息、待补充内容与转录疑点 |
| 用户补充 | 可选展示会后备注，与会上原话及决定明确区分 |

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| SUM-01 | P0 | 校对完成后自动生成纪要；默认中文，可选择英文。 |
| SUM-02 | P0 | 使用指定版本的校对稿、人工修改和人物映射，不误用旧版本。 |
| SUM-03 | P0 | 关键决策和行动项关联一个或多个原文片段，点击可定位对应发言。 |
| SUM-04 | P0 | 没有明确负责人的行动项标记“未明确”；没有明确截止日期时不编造日期。 |
| SUM-05 | P0 | 不能仅根据发言人身份把任务分配给该发言人；不得把建议写成决策。 |
| SUM-06 | P0 | 转录有缺失、待补转或重要疑点时，纪要中显示相关限制。 |
| SUM-07 | P0 | 支持人工编辑纪要及重新生成，保留旧版本。 |
| SUM-08 | P0 | 上游转录、人物归属或重要备注变化后，标记已有纪要可能需要更新，不静默覆盖。 |
| SUM-09 | P0 | 人工编辑过的纪要重新生成时形成新版本，用户可以比较与选择。 |

对于长会议，允许按议题或时间段提取要点后汇总，但最终结论仍须关联原始发言，不能只引用中间摘要作为证据。

### 5.8 模型与推理强度

| 设置项 | 默认值 | 可调整范围 |
|---|---|---|
| 校对模型 | GPT-6 Astra | GPT-6 Astra、GPT-5.6 Sol、Terra、Luna |
| 校对推理强度 | medium | 所选模型及 Bedrock 接口实际支持的档位 |
| 总结模型 | GPT-6 Astra | GPT-6 Astra、GPT-5.6 Sol、Terra、Luna |
| 总结推理强度 | medium | 所选模型及 Bedrock 接口实际支持的档位 |
| 纪要语言 | 中文 | 中文、英文 |

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| MODEL-01 | P0 | 全局设置分别保存校对和总结的模型、推理强度；用户可针对当前会议覆盖。 |
| MODEL-02 | P0 | 每次处理开始时固定所使用的配置；处理中修改全局设置不改变已发出的请求。 |
| MODEL-03 | P0 | 默认明确发送 medium，不依赖服务端可能不同的默认值。 |
| MODEL-04 | P0 | 根据模型与接口支持情况展示推理档位，不向不支持的模型发送无效参数。 |
| MODEL-05 | P0 | 模型、区域或账户权限不可用时显示原因；不静默切换模型、推理强度或跨区域路由。 |
| MODEL-06 | P0 | 每次校对、总结记录模型、推理强度、区域／路由、输入版本及生成时间。 |
| MODEL-07 | P0 | 模型设置只影响校对和总结，不改变 Transcribe 的实时识别配置。 |
| MODEL-08 | P1 | 展示可获得的调用用量、耗时和费用估算，并注明价格日期及估算范围。 |

GPT-6 Astra 官方文档列出的推理档位包括 low、medium、high、xhigh、max。其他模型与具体 Bedrock 接口的参数兼容性，在接入阶段验证。更高推理强度通常增加耗时与费用，不保证每场会议的结果都更好。

### 5.9 历史、搜索与导出

| 编号 | 优先级 | 需求与验收要求 |
|---|---|---|
| DATA-01 | P0 | 本地保存会议标题、时间、状态、发言人、转录、备注和纪要版本。 |
| DATA-02 | P0 | 历史列表支持按标题或文本内容检索，并显示进行中、待处理、失败或完成状态。 |
| DATA-03 | P0 | 会议详情可切换纪要、校对稿、原始转录和人物信息。 |
| DATA-04 | P0 | 支持导出 Markdown、TXT；可选择纪要或转录，包含必要的时间和人物标签。 |
| DATA-05 | P0 | 导出含人工备注时明确标记；不得将备注混入原始发言。 |
| DATA-06 | P0 | 支持删除本地会议资料，并说明关联音频、转录和纪要的删除范围。 |
| DATA-07 | P0 | 应用异常退出后可恢复已保存的内容，并标记被中断的记录或处理任务。 |
| DATA-08 | P1 | 增加 PDF、Word 导出及更丰富的历史筛选。 |

本地删除不应被描述为已经删除云服务侧可能存在的日志或按服务条款保留的数据。

## 6. 界面结构

### 6.1 菜单栏面板

- 当前状态：未记录、记录中、已暂停、处理异常。
- 当前会议标题与录音时长。
- 开始、暂停、恢复、结束并生成纪要。
- 打开当前会议、历史记录和设置。

### 6.2 开始记录面板

- 会议标题。
- 会议应用与麦克风选择。
- 识别语言：默认中英混合。
- 权限及连接状态。
- 当前校对、总结模型配置概览。
- 明确的“开始记录”按钮。

### 6.3 会议窗口

- 顶部：会议标题、状态、时长、暂停和结束按钮。
- 采集状态：会议应用声音、麦克风分别展示音量和异常。
- 正文：按时间排列的转录，显示发言人和临时／确定状态。
- 人物面板：姓名、角色、备注、合并与归属调整。
- 发言操作：编辑、备注、标记重点、定位时间；音频可用时回听。
- 不强制持续自动滚动；用户查看历史段落时可保持位置，并提供返回最新发言入口。

### 6.4 会后查看窗口

- 纪要、校对稿、原始转录切换。
- 校对修改前后对比与待确认项。
- 纪要引用定位。
- 版本选择、重新校对、重新生成与导出。
- 明确区分人工备注、人工编辑和 AI 生成内容。

### 6.5 设置

- 默认识别语言、纪要语言。
- 校对和总结各自的模型、推理强度。
- AWS profile、服务区域及连接状态。
- 会议检测提醒。
- 音频缓存、保留期限和存储位置。
- 数据删除与诊断信息入口。

## 7. 数据与版本规则

以下为概念数据模型，不限定具体数据库表结构。

| 对象 | 主要内容 |
|---|---|
| 会议 | 标题、开始结束时间、采集来源、语言、状态 |
| 音轨与转录会话 | 来源、设备、服务会话标识、会议内时间偏移、断点和缺口 |
| 原始转录片段 | 稳定本地标识、原文、起止时间、服务结果标识、原始说话人标签 |
| 发言人 | 会议内稳定标识、显示名、角色、人物备注、与服务标签的映射 |
| 人工编辑 | 修改对象、修改前后内容、范围、时间和当前生效状态 |
| 人工备注 | 关联会议／人物／发言，备注内容及时间 |
| 校对版本 | 输入版本、修改建议、生效修改、未确认项及模型配置 |
| 纪要版本 | 输入校对版本、正文、行动项、原文引用、人工编辑及模型配置 |
| 处理任务 | 阶段、进度、错误、重试状态和可获得的用量信息 |

必须遵守以下规则：

1. 已确定的原始转录不可被 LLM 或人工编辑覆盖；用户删除会议属于独立的数据删除操作。
2. 人工修改、校对修改和人物映射分别保存，可以解释当前文本如何形成。
3. 时间以同一场会议为基准。暂停、断线和会话重建不能导致时间错位。
4. 服务说话人编号必须与对应转录会话一起解释，不能跨会话直接复用。
5. 纪要引用绑定稳定的片段标识，并保留生成时输入版本，避免文本更新后引用失真。
6. 重新生成产生新版本；旧版本与新版本的模型和输入来源均可查询。
7. 人工修改使派生结果过时时，显示更新提示，不自行覆盖用户内容。

## 8. 技术方案与约束

### 8.1 推荐实现

| 层次 | 方案 |
|---|---|
| 界面与系统交互 | SwiftUI；必要时使用 AppKit |
| 会议应用音频 | Core Audio Process Tap，按应用进程或进程组采集 |
| 麦克风 | Apple 原生音频采集能力，单独处理设备选择和状态 |
| AWS 接入 | 优先使用 AWS SDK for Swift；具体 Bedrock 接口依据模型与推理参数兼容性确定 |
| 本地存储 | SQLite；音频使用独立文件并关联索引 |
| 结构划分 | 音频、会议状态、转录、校对、总结、存储和界面分别组织 |

### 8.2 双路转录

推荐使用两个独立的 Transcribe 流：

- 麦克风流：默认标记为“我”。
- 会议应用流：启用远端说话人分离。

两路结果根据会议时间合并。这种方式会增加转录用量，成本不能按单条流计算。是否可在当前区域同时使用中英混合识别及说话人分离，是首个验证阶段的关键问题。

### 8.3 断线与补转

- 实时转录恢复后继续处理新音频。
- 缺口音频作为独立任务处理，不能假设流式接口允许无限加速回放。
- 补转结果必须与实时结果去重，并保留原始时间。
- 如果补转选择 Transcribe 批处理，需额外设计 S3 临时存储、权限与清理；S3 不属于已确认的默认架构。
- 具体补转机制通过可行性验证后确定。

### 8.4 AWS 凭证与模型路由

- 复用用户已有 AWS 凭证配置，支持选择对应 profile；不把密钥写入项目、会议数据或导出文件。
- 图形应用不能假定拥有与终端完全相同的环境变量，需验证实际凭证读取与刷新行为。
- 凭证失效与无权限需展示可理解的原因和恢复入口。
- Transcribe 与 Bedrock 的区域设置可分别管理。
- 模型显示名、实际 model ID、端点、推理路由和参数映射分开管理。
- 不假定四个模型在每个区域、每种接口上都支持相同参数。
- 应用侧验证 LLM 输出，不依赖所有模型都提供相同的服务端结构化输出能力。

### 8.5 数据流与费用

- 音频发送至 AWS Transcribe。
- 用于校对和总结的文本、人物信息、术语及必要备注发送至 Bedrock。
- 本地会议资料与云端服务处理是不同概念，界面不能声称整个流程均在本地完成。
- 费用主要由两路转录时长、补转、校对、总结及重复生成产生。
- 在途重试与重复请求需受控，避免无限重试或无提示地重复处理整场会议。
- 诊断日志默认记录状态、错误和必要标识，避免默认包含完整会议正文或凭证。

## 9. 异常处理

| 异常 | 预期行为 |
|---|---|
| 未授予系统音频／麦克风权限 | 明确指出缺少哪项权限，提供系统设置指引，不显示正在正常采集 |
| 系统音频无数据但麦克风正常 | 单独提示远端音轨异常，保留已有内容 |
| 会议软件静音 | 不假设本应用麦克风同步静音，保持独立状态可见 |
| 耳机或麦克风断开 | 显示来源变化，恢复支持的设备或要求重新选择，不隐瞒缺口 |
| 网络断开 | 根据缓存设置继续保存或标记丢失，显示待转录区间 |
| AWS 凭证失效 | 保留资料，停止无效重试，提示恢复凭证 |
| 限流或服务暂时不可用 | 有上限地重试并显示状态，不静默更换模型 |
| 流超过时长限制或必须重建 | 新建会话、保留时间关系，重新管理说话人标签 |
| 转录最后结果未返回 | 标记收尾未完成，允许重试或明确继续生成不完整纪要 |
| 校对修改无法匹配原文 | 拒绝应用该修改，保留原始内容并提供重试／人工处理 |
| 校对或总结中断 | 保留已保存版本，从失败阶段重试 |
| 系统睡眠或应用异常退出 | 恢复已保存内容，标记中断区间，不假装期间持续记录 |
| 音频缓存已过期删除 | 保留文字引用，回听入口提示音频不可用 |

## 10. 质量目标与验收

### 10.1 功能验收

| 编号 | 场景 | 通过条件 |
|---|---|---|
| AC-01 | 未确认的疑似会议 | 无音频采集、缓存或上传；忽略提醒不会开始记录 |
| AC-02 | Teams 与 Zoom | 两个客户端分别完成两路采集、实时转录、校对、纪要的完整流程 |
| AC-03 | 语言模式 | 默认中英混合正确保存；中文、英文模式可切换并正确建立对应识别请求 |
| AC-04 | 远端多人发言 | 使用至少三名远端发言人的交替发言样本，产生可管理的多个标签，并完成命名、合并和归属纠正 |
| AC-05 | 备注隔离 | 人物备注、发言备注不会伪装成会上原话出现在原始转录中 |
| AC-06 | 暂停与恢复 | 暂停区间无新音频采集；恢复后的时间与原有内容对应 |
| AC-07 | 设备切换 | 耳机插拔及麦克风切换后能恢复支持的采集；任何缺口均明确可见 |
| AC-08 | 断网恢复 | 缓存开启时保留缺口音频，补转后无重复段落和时间错位；缓存关闭时显示缺失 |
| AC-09 | 人工修改保护 | 校对、重试和重新生成不会覆盖已保护的人工转录修改 |
| AC-10 | 保守校对 | 数字、日期、否定、条件与承诺程度的测试样本不被无依据地改成确定事实 |
| AC-11 | 纪要依据 | 每条关键决策和行动项能定位到支持它的原文；未明确负责人和日期不被补造 |
| AC-12 | 模型配置 | 两阶段默认 Astra / medium；可独立切换；不支持的组合被阻止且说明原因 |
| AC-13 | 历史版本 | 修改、重新生成后仍可访问旧版本及其输入来源、模型配置 |
| AC-14 | 持久化 | 强制关闭后重启，已确认保存的转录和人工编辑可恢复 |
| AC-15 | 导出 | Markdown、TXT 与选择的版本一致，人物标签、时间和人工备注类别正确 |

### 10.2 稳定性与体验目标

以下是建议的验证目标，不是已经测得的服务承诺：

- 在目标 Mac 上完成至少一次连续两小时的会议测试，确认音频、界面和持久化没有持续积压。
- 音频采集与写入不依赖界面刷新；长会议查看、滚动和编辑不应阻断采集。
- 操作后立即提供开始准备、暂停、结束或处理中的可见反馈。
- 分别测量首次字幕延迟、确定文本延迟、会后校对耗时和纪要生成耗时；不混成一个笼统的“响应时间”。
- 记录长会议的 CPU、内存、磁盘和耗电表现，判断是否适合与 Teams、Zoom 同时常驻。
- 验证关闭主窗口、切换前后台、锁屏与系统睡眠的实际行为。

### 10.3 识别与总结质量评估

建立中文、英文、中英混合三类样本，覆盖产品名、缩写、数字、否定句、多人交替和抢话。

- 中文转录评估字符错误，英文评估词错误；中英混合需统一分词与统计口径。
- 说话人评估归属错误，并单独记录重叠发言的表现。
- 校对同时评估“修正了多少错误”和“新增了多少错误”。
- 纪要评估决策遗漏、无依据结论、负责人错误、日期错误和引用有效性。
- 质量门槛在技术验证阶段基于固定样本确定，不能仅以一次成功演示作为通过依据。

核心能力组合、外放效果及质量门槛若无法达到预期，应先调整方案并更新 PRD，再承诺相应使用场景。

## 11. 开发阶段建议

### M0：验证关键能力

- 在目标 Mac 上验证 Teams、Zoom 的指定应用采集及麦克风并行采集。
- 验证耳机、外放、设备切换与录音权限。
- 验证中英混合识别、远端说话人分离和双路并发。
- 验证四个 Bedrock 模型的可用区域、访问权限、推理参数与调用接口。
- 确定断网补转方式、样本集和质量门槛。

交付：可行性结论、兼容性记录、质量基线和需要调整的需求。此阶段通过后再扩大界面开发范围。

### M1：可持续记录的原生应用

- 菜单栏与手动记录。
- 两路音频状态、暂停恢复、实时转录和增量保存。
- 发言人命名、备注、合并及归属纠正。
- 断网、设备变化和异常退出恢复。

### M2：校对与纪要闭环

- 术语表、校对差异、人工修改保护。
- 两阶段模型与推理强度设置。
- 纪要、原文引用、行动项及历史版本。

### M3：首版完善与验收

- 疑似会议提醒、历史搜索和 Markdown／TXT 导出。
- 数据保留、删除、缓存清理与完整异常提示。
- 两小时稳定性测试和固定样本质量评估。
- 完成目标分发方式所需的签名、权限和安装验证。

## 12. 待确认或待验证事项

| 事项 | 当前状态 | 确定时机 |
|---|---|---|
| AWS profile 与服务区域 | 用户已有凭证，尚未检查具体配置 | M0 |
| Bedrock 四个模型及推理参数的账户可用性 | 官方列出模型；未验证此账户 | M0 |
| 中英混合＋远端说话人分离组合 | 单项服务能力有文档；组合效果未实测 | M0 |
| 外放回声处理与支持程度 | 耳机优先验证，外放方案未定 | M0 |
| 断线补转是否需要 S3 批处理 | 尚未确定 | M0 |
| 本地音频缓存默认是否开启 | 建议开启可恢复缓存，尚未最终确认 | M1 前 |
| 音频默认保留期限及失败任务保留规则 | 尚未确定；避免自动删除唯一的待补转音频 | M1 前 |
| 产品名、图标与视觉样式 | 暂用功能名称 | 界面设计阶段 |
| 识别与说话人质量阈值 | 待固定样本及基线测试 | M0 |
| 首版支持的会议客户端版本 | 优先用户实际安装版本，尚未检查 | M0 |
| 分发范围 | 当前为个人本机使用；团队分发或商店上架未确定 | 发布前 |

这些事项不影响当前 PRD 的整理，但涉及对应功能承诺前必须明确。

## 13. 参考资料

以下资料在前期设计讨论中查阅。服务参数、区域和账户权限以实施时的官方资料及验证结果为准。

1. [Apple：Capturing system audio with Core Audio taps](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps)
2. [AWS Transcribe：流式转录](https://docs.aws.amazon.com/transcribe/latest/dg/streaming.html)
3. [AWS Transcribe：支持语言及语言相关功能](https://docs.aws.amazon.com/transcribe/latest/dg/supported-languages.html)
4. [AWS Transcribe：StartStreamTranscription](https://docs.aws.amazon.com/transcribe/latest/APIReference/API_streaming_StartStreamTranscription.html)
5. [AWS Transcribe：说话人分离](https://docs.aws.amazon.com/transcribe/latest/dg/diarization.html)
6. [AWS Transcribe：双声道识别](https://docs.aws.amazon.com/transcribe/latest/dg/channel-id.html)
7. [Amazon Bedrock：OpenAI 模型列表](https://docs.aws.amazon.com/bedrock/latest/userguide/model-cards-openai.html)
8. [Amazon Bedrock：GPT-6 Astra 模型说明](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-openai-gpt-6-astra.html)
9. [OpenAI：GPT-6 Astra 模型与推理档位](https://developers.openai.com/api/docs/models/gpt-6-astra)
10. [OpenAI：通过 Amazon Bedrock 使用模型](https://developers.openai.com/api/docs/guides/amazon-bedrock)

## 14. 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-09-17 | 汇总原生 Mac 方案、确认后记录、混合语言、说话人管理、人工备注、双阶段 LLM、模型设置、异常恢复及验收标准 |
